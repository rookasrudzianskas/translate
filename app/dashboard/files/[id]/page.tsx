import React from 'react';

const ChatToFilePage = ({params: { id }}: { params: { id: string }}) => {
  return (
    <div>
      <h1>Chat to file {id}</h1>
    </div>
  );
};

export default ChatToFilePage;
// by Rokas with ❤️
